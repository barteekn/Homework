import { StyledFooter } from './Footer.styled';
const Footer = ({footer}) => {
    return ( 
    
        <StyledFooter>
                {footer}
        </StyledFooter>
       
  )
  } 

  export default Footer;