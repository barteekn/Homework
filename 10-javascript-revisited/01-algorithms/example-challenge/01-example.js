// What will be the output if we log this into console?

const arr = ["Nigeria", "Africa", 400, "English"]

const country = arr[0]
const continent = arr[1]
const population = arr[2]
let language = arr[3]

console.log(`${country} is located in ${continent} and has a population of ${population} and speaks ${language}`)